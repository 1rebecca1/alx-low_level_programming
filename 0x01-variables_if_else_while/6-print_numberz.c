#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
int numb;
for (numb = 0;  numb < 10; numb++)
	putchar((num % 10) + '0');
{
putchar (numb);
}
putchar ('\n');
return (0);
}
