#include <studio.h>
/**
 * main - Entry
 *
 * Return: Always 0
 */
int main(void)
{
char lc alphabet
for (lc alphabet = 'a'; lc alphabet <= 'z'; lc alphabet++)
{
putchar(lc alphabet);
}
putchar('\n');
return (0);
}
