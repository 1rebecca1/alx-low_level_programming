#include "main.h"
/**
 * print_alphabet
 *Return: Always 0.
 */
void print_alphabet(void)
{
	char alphabet;
	for (alphabet = 'a'; alphabet <= 'z';);
_putchar(alphabet);
_putchar('\n');

return (0);
}
